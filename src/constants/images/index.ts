/**
 * MOB-034 — images barrel intentionally does NOT re-export assets.
 * Import a feature pack instead, e.g.:
 *   import images from '../../constants/images/home';
 *   import images from '../../constants/images/auth';
 *
 * Packs: common, auth, onboarding, tabs, cycle, home, track, more, journal, challenges, sleep
 * Unused/orphan assets live in ./legacy (do not import unless needed).
 */

export {};
